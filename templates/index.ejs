<!DOCTYPE html>
<html>
    <head>
        <title>Главная</title>
        <meta charset="utf-8">
    </head>
    <body>
        <h1>MEGASHOP</h1>
        
        <h2>Категории</h2>
        <ul>
            <% for (let i = 0; i < categories.length; i++) {%>
                <li><%=categories[i].name %></li>
            <% } %>
        </ul>
    </body>
</html>